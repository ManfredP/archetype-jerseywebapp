Maven Archetype for Jersey Webapp
=================================
This is a Maven Archetype for Java REST projects using Jersey as the JAX-RS implementation. JSON support is included
using the Jackson libraries.
